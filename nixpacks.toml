[phases.setup]
nixPkgs = ["python312", "tesseract", "poppler_utils", "zlib", "libjpeg", "libpng"]

[phases.build]
cmds = ["pip install -r requirements.txt", "python -m spacy download en_core_web_sm"]

[start]
cmd = "uvicorn src.api.main:app --host 0.0.0.0 --port $PORT"
